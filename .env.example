# Public URL used for links and metadata
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Email notifications for waitlist + pilot intake
RESEND_API_KEY=
RESEND_FROM_EMAIL=hello@flowclaw.io
NOTIFY_EMAIL=ops@flowclaw.io

# AWS deployment defaults
AWS_REGION=us-east-1

# Future persistence placeholders
DYNAMODB_RUNS_TABLE=flowclaw-agent-runs
DYNAMODB_APPROVALS_TABLE=flowclaw-agent-approvals
DYNAMODB_WORKFLOWS_TABLE=flowclaw-workflows
